version: "3.8"

services:
  app:
    build: .
    container_name: billing-service-app
    ports:
      - "4001:4001"
      - "9001:9001"
    networks:
      - internal

    volumes:
      - .:/apps
networks:
  internal:
   external: true